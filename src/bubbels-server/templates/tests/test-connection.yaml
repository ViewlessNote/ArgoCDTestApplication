apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bubbels.fullname" . }}-test-connection"
  labels:
    {{- include "bubbels.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bubbels.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
