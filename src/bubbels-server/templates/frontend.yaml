# Frontend Service
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  selector:
    app: frontend
  ports:
  - port: {{.Values.frontendDeployment.ports.inContainerPort}}
    targetPort: {{.Values.frontendDeployment.ports.outContainerPort}}