# Backend Service
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
  - port: {{.Values.backendDeployment.ports.inContainerPort}}
    targetPort: {{.Values.backendDeployment.ports.outContainerPort}}
